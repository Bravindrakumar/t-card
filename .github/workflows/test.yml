on: push
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: setup python
        run: |
          python .github/shell/determinefolder.py
          echo "hi all 20"
        
          - 
        
    
